# portfolio-site

静态作品集站，已加入简易设置入口。

## 用法

- 页面右下角有一个设置按钮。
- 输入密码后可以修改页面文案和板块开关。
- “保存到当前浏览器” 只会在当前设备生效。
- “导出配置” 会下载 `site-config.json`。
- 把新的 `site-config.json` 上传到仓库根目录并提交后，GitHub Pages 会重新发布，别人访问链接时也能看到更新。

## 默认密码

`123456`

可在 `site-config.json` 中修改字段：

```json
"adminPassword": "123456"
```
